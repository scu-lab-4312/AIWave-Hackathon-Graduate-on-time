# AgentCore infrastructure

Store reviewed deployment configuration and generated-resource notes here. Do not commit credentials, bearer tokens, or secret values.

The existing `.bedrock_agentcore.yaml` belongs to the legacy starter toolkit deployment. New specialist agents should use the current `@aws/agentcore` project configuration generated by the CLI.
